spring:
  profiles:
    active: dev

server:
  servlet:
    context-path: /inforelease
  port: 38081
  tomcat:
    uri-encoding: UTF-8
  
mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.zhuxn.inforelease